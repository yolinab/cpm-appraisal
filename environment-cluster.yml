name: cpm
channels:
  - pytorch
  - nvidia
  - defaults
dependencies:
  - python=3.11
  - pytorch::pytorch
  - pytorch::torchvision
  - pytorch::pytorch-cuda=12.1   # match DelftBlue GPU driver; change to 11.8 if needed
  - pip
  - pip:
    - -e ".[langgraph,local,dev]"
